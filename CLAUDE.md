# Emoji 套圈小游戏

## 项目概述

单文件 HTML 套圈小游戏。目标用 emoji 表示，圈为 Canvas 绘制的空心渐变圆环。平面俯视视角，操作方式为拖拽瞄准 + 按住蓄力（循环增减） + 松手投掷。

## 文件结构

```
D:/emoji-ring-toss/
├── index.html                    # 单文件游戏（内嵌 CSS + JS）
├── index-套圈小游戏备份.html      # 备份副本
└── CLAUDE.md                     # 本文件
```

## 技术栈

- 纯 HTML/CSS/JS，零依赖
- Canvas 2D 渲染
- Web Audio API 音效
- 响应式缩放适配不同屏幕

## 核心架构

### 状态机
```
START → PLAYING → LEVEL_COMPLETE → (下一关 或 GAME_OVER)
```

### JS 模块（均在 index.html 内）

| 模块 | 功能 |
|------|------|
| CONFIG | 全局常量配置（含配色、关卡、圈颜色） |
| AudioEngine | Web Audio 音效封装（含随机音高变化） |
| LevelManager | 10 关布局定义与目标生成（屏幕坐标） |
| GameState | 状态管理与数据（含选中圈颜色索引） |
| Input | Pointer 事件处理（鼠标+触摸） |
| Physics | 平面飞行插值 + 碰撞检测 |
| Renderer | Canvas 绘制（场地、目标、圈、粒子、HUD） |
| Particles | 每次命中触发烟花 + 分数弹出 |
| UI | 覆盖层（开始/过关/结算）+ 鼓励文字 |

### 平面坐标系统
- 不使用透视变换，所有坐标均为屏幕像素坐标
- 矩形圆角场地：fieldX/Y/W/H 定义
- 圈起点：场地底部中央
- 目标排列：以场地中心为基准的各种布局

### 蓄力循环
- 按住期间 power 在 0↔1 之间往复（三角波）
- 周期：cycleMs=1800（0→1 需 1.8s，完整来回 3.6s）
- 松手取当前 power 值（最低 0.10）

### 碰撞判定
- 圈落地后检测目标中心是否在环的扩展范围内
- 范围：内半径×0.50 ~ 外半径×1.50
- 完美判定：距离 < 28px

### 计分
- 基础分 = 100 × (1 / 目标缩放比例)
- Combo：连续命中 ×1~×5
- 完美：在 combo 上再 ×2
- 剩余圈数加分

### 圈颜色
- 粉色（默认）、蓝色、绿色、黄色
- 左下角颜色选择器切换
- 空心渐变圆环，高光和阴影模拟立体感

### 配色方案
- 主背景：#e2c4cc（柔和浅粉）
- 场地：#f0dce3（更浅粉）
- 面板：rgba(255,242,247,0.94)
- 文字主：#4a3540，辅：#8a6a78
- 按钮：#d4a8b8 → #c098a8 渐变

### 关卡（10 关）

| 关 | 目标数 | 大小 | 分散 | 排列 |
|----|--------|------|------|------|
| 1 | 10 | 5 | 1.15 | 130 | 网格2 |
| 2 | 10 | 6 | 1.10 | 145 | 网格 |
| 3 | 10 | 6 | 1.05 | 160 | 菱形 |
| 4 | 10 | 7 | 0.97 | 175 | 交错 |
| 5 | 10 | 7 | 0.90 | 190 | 圆形 |
| 6 | 10 | 8 | 0.85 | 205 | 两组 |
| 7 | 10 | 9 | 0.82 | 220 | 散点 |
| 8 | 10 | 9 | 0.85 | 220 | 锯齿 |
| 9 | 10 | 10 | 0.85 | 235 | 环绕 |
| 10 | 10 | 10 | 0.87 | 240 | 混合 |

## 修改记录

### 2026-05-17 — 第三版修复
- **BGM 重写**：卡农旋律增加低声部、和声层、更完整的主旋律线，使用 ADSR 包络模拟钢琴音色
- **静音修复**：改用 masterGain 节点，静音时增益设为 0，彻底消除残留音频
- **移动端全面适配**：
  - Canvas 根据视口动态计算尺寸，不再固定 900×600
  - 所有布局参数（场地、圈大小、飞行距离等）按屏幕比例缩放
  - 桌面端保持 3:2 固定比例，移动端全屏自适应
  - 监听 resize 事件实时重算布局
- **取消拖拽瞄准**：改为自动摆动瞄准（正弦波，周期 2.2s，幅度 ±0.38π）
  - 操作简化为：按住蓄力 → 瞄准自动摆动 → 松手投掷
  - 移动端只需单手操作，体验大幅提升
- **emoji 间隔**：minTargetDist 提升到 40px（缩放后），spread 上调 10-15

### 2026-05-17 — 第三版改动
- 每关 emoji 数量统一增加到 10 个
- 新增过关条件：命中数 ≥ passHits（5→6→6→7→7→8→9→9→10→10）
- 命中不足判定不过关，可重试本关或返回选关列表
- 新增选关列表，玩家可自由选择已通关关卡
- 新增卡农 D 大调钢琴背景音乐（Web Audio 生成，跟随静音开关）
- emoji 全部换为跨平台渲染一致的版本（🐱🐶🍩🍰🌸🌺 等）
- 目标间距阈值提高到 34px，布局 spread 上调，确保不重叠
- 移动端全面适配：viewport、触摸目标 ≥44px、响应式断点、防缩放/长按/双击
- 新增 HUD "需命中"指示器

### 2026-05-17 — 项目初始化
- 确定游戏方案与需求
- 创建项目结构
- 编写完整游戏代码（45度透视版）

### 2026-05-17 — 第二版大改
- 配色改为低明度中饱和度粉色系（主色#e2c4cc）
- 视角从45度伪3D改为平面俯视
- 去掉w2s()透视变换，全部改为屏幕坐标
- 圈增加粉/蓝/绿/黄四色可选
- 蓄力改为循环增减（三角波，周期1.8s）
- 每次命中都放烟花粒子（不限于完美）
- 命中判定放宽：碰撞范围扩大，目标size上调，完美阈值28px
- 过关增加随机鼓励文字（"慧慧宝宝真棒！！"等）
- 音效增加随机音高变化，增加sfxGameOver
- 飞行时间从850ms降到600ms
